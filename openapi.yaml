openapi: 3.1.0
info:
  title: Chatterisk
  version: 0.1.0
  description: >
    Canonical HTTP contracts live as one OpenAPI YAML per domain under
    packages/openapi-core/src/. This file is a pointer only.
servers:
  - url: https://api.chatterisk.local
paths: {}
x-domains:
  - identity.yaml
  - projects.yaml
  - comments.yaml
  - aggregates.yaml
  - alerts.yaml
  - dossiers.yaml
  - external-links.yaml
