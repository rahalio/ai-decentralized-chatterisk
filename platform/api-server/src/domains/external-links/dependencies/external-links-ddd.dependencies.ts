/**
 * ExternalLinks DDD Dependencies - Composition root
 *
 * Generated by Dependencies Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: external-links
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExternalLinks Domain Module - Composition root
 *
 * DDD: Composition root for external-links domain. Exposes repos (ports) and use cases
 * grouped by aggregate. No orgId at build time — use execution context at call time.
 */

import {
  ExternalLinkRepositoryAdapter,
} from "@chatterisk/adapters/external-links";
import { getIdGeneratorService } from "@chatterisk/adapters";
import type { AdapterDynamoDBClient } from "@chatterisk/adapters";
import { executionContextService } from "../../../lib/execution-context.service.js";
// Use-case constructors (concrete)
import {
  ExecuteCreateExternalLink,
  ExecuteDeleteExternalLink,
  ExecuteGetExternalLink,
  ExecuteListExternalLinks,
} from "@chatterisk/services/external-links/usecases";
// Ports (interfaces)
import type {
  ExternalLinkRepository,
} from "@chatterisk/services/external-links/ports";

export interface ExternalLinksDomainModule {
  repos: {
    externalLinks: ExternalLinkRepository;
  };
  useCases: {
    externalLinks: {
      create: ExecuteCreateExternalLink;
      delete: ExecuteDeleteExternalLink;
      get: ExecuteGetExternalLink;
      list: ExecuteListExternalLinks;
    };
  };
}

export function buildExternalLinksDomainModule(
  dynamoClient: AdapterDynamoDBClient,
): ExternalLinksDomainModule {
  // Adapters implementing ports (adapters encapsulate Ddb)
  const repos = (() => {
    const obj: {
      externalLinks?: ExternalLinkRepository;
    } = {};

    obj.externalLinks = new ExternalLinkRepositoryAdapter(dynamoClient);
    return {
      externalLinks: obj.externalLinks!,
    };
  })();

  const executionContext = executionContextService;
  const idGenerator = getIdGeneratorService();

  // Use cases depend only on ports (repos.*)
  const useCases = {
    externalLinks: {
      create: new ExecuteCreateExternalLink(executionContext, idGenerator, repos.externalLinks),
      delete: new ExecuteDeleteExternalLink(executionContext, idGenerator, repos.externalLinks),
      get: new ExecuteGetExternalLink(executionContext, idGenerator, repos.externalLinks),
      list: new ExecuteListExternalLinks(executionContext, idGenerator, repos.externalLinks),
    },
  };
  return { repos, useCases: useCases as ExternalLinksDomainModule["useCases"] };
}
