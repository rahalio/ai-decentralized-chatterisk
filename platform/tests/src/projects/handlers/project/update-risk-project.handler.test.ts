/**
 * UpdateRiskProject handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: projects
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { updateRiskProject } from "@chatterisk/api-server/domains/projects/handlers";
import type { ProjectsDDDDependencies } from "@chatterisk/api-server/domains/projects";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@chatterisk/tests/shared";
import { PROJECTS_USE_CASES } from "../__factories__/use-cases.js";

describe("UpdateRiskProject", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: ProjectsDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<ProjectsDDDDependencies>(
      PROJECTS_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with updated entity", async () => {
    const entity = { id: 'mock-id', orgId } as any;
    const input = {} as any;
    const updatedEntity = { ...entity, ...input };
    const useCaseResult = { data: updatedEntity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    mockRequest.params = { projectId: entity.id };
    mockRequest.body = input;
    vi.spyOn(mockDeps.executeUpdateRiskProject, "execute").mockResolvedValue(useCaseResult);

    await updateRiskProject(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeUpdateRiskProject.execute).toHaveBeenCalledWith(expect.objectContaining({ projectId: entity.id }));
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

});
